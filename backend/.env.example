# =============================================
# Supersonic Music Backend - Environment Variables
# =============================================
# For Railway deployment, set these in the Railway dashboard:
# https://railway.app/project/YOUR_PROJECT > Variables
# =============================================

# Port for the server (Railway sets PORT automatically)
PORT=3001

# Region for YouTube content (IN = India, US = USA, etc.)
REGION=IN

# Node environment
NODE_ENV=production

# Allowed Origins for CORS (comma-separated)
# For production, set your frontend URL:
# Example: https://your-app.vercel.app,https://your-frontend.up.railway.app
# Use * to allow all origins (not recommended for production)
ALLOWED_ORIGINS=*

# =============================================
# Railway Deployment Notes:
# =============================================
# 1. Railway automatically provides PORT
# 2. Set ALLOWED_ORIGINS to your frontend URL in production
# 3. Python packages (yt-dlp, ytmusicapi) are installed via nixpacks.toml
# 4. The /health endpoint is used for health checks
# =============================================
